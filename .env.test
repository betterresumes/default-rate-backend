# Test Email Configuration
# Copy this to .env.test and update with your actual GoDaddy email credentials

# Email Configuration (GoDaddy Gmail SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=mohit.saggi@accunode.ai
SMTP_PASSWORD=your-app-specific-password
SMTP_TLS=true
SMTP_SSL=false

# Email Settings
EMAIL_FROM=noreply@yourdomain.com
EMAIL_FROM_NAME=AccuNode Contact
EMAIL_TO=contact@yourdomain.com
EMAIL_SUBJECT_PREFIX=[AccuNode Contact]

# Basic app settings
ENVIRONMENT=development
DEBUG=true
